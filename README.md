## 🧪 Hands-On Activities

### 1️⃣ Monitoring EC2 Instance with htop
Verified CPU usage, memory consumption and active services (systemd, sshd, amazon-ssm-agent) inside a Linux EC2 instance.

### 2️⃣ SSH Connection via AWS Console
Connected to EC2 instance using private key (.pem) and validated secure remote access.

### 3️⃣ Compiling C Code on EC2
Compiled a C program using GCC, adjusted permissions with chmod 777, and executed the binary successfully.

### 4️⃣ Instance Shutdown
Used `sudo shutdown -h now` to safely stop the EC2 instance and verified termination behavior.

### 5️⃣ Instance State Validation
Confirmed instance status changes in AWS Management Console (Running → Stopped).
